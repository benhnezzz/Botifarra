# Copia este archivo a ".env" y completa tus propias claves.
# ".env" está en .gitignore: nunca se sube al repositorio.

# Usadas por .wa (comando checkWhatsApp) para verificar números
NUMVERIFY_KEY=
ABSTRACTAPI_KEY=

# Clave de la API de Anthropic (Claude), usada por .rpgcrearclase y
# .rpgcrearraza para generar contenido nuevo del RPG con IA.
# Consíguela en https://console.anthropic.com/settings/keys
ANTHROPIC_API_KEY=
# Opcional: modelo a usar (por defecto claude-sonnet-5)
ANTHROPIC_MODEL=

# Alternativa a config.js para definir el owner por variables de entorno
OWNER_NUMBER=
OWNER_LIDS=
PAIRING_NUMBER=
